# Copia questo file in ".env" e metti la tua chiave.
# Il file .env NON viene mai messo su git (vedi .gitignore).
#
# La funzione e' facoltativa: senza chiave, Rubi usa le spiegazioni scritte a
# mano e l'app funziona identica, anche senza rete.
EXPO_PUBLIC_GEMINI_API_KEY=
